kali_core_help(){

	echo
	echo "Usage: kali-core [OPTIONS]"
	echo 
	echo "Example: kali-core -v"
	echo "         kali-core --version"
	echo "         kali-core install"
	echo "         kali-core uninstall"
	echo "         kali-core test"
	echo
	echo "Options:"
	echo "  -h, --help,      help         Display helpful information"
	echo "  -v, --version,   version      Display the current version of this application"
	echo
	echo "Commands:"
	echo "  -i, --install,   install      Use this command to install kali-core"
	echo "  -u, --uninstall, uninstall    Use this command to uninstall kali-core"
	echo "  -t, --test,      test         Use this command to test kali-core"
	echo

}